# Newhorse Environment Configuration

# API Server
API_PORT=8999

# Database (SQLite for development)
DATABASE_URL=sqlite:///data/newhorse.db

# Projects storage
PROJECTS_ROOT=data/projects

# User-created agent templates
AGENTS_ROOT=data/agents

# Environment: dev, prod
THS_TIER=dev

# Optional: Redis for multi-worker WebSocket support
# REDIS_URL=redis://localhost:6379/0

# Optional: Custom Claude CLI path
# CLAUDE_CLI_PATH=/usr/local/bin/claude

# Encryption key for API key storage (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=
