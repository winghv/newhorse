# Newhorse 平台功能增强设计

日期: 2026-02-05

## 概述

本设计涵盖三个核心功能增强，使 Newhorse 平台从基础对话工具升级为完整的 AI Agent 开发平台：

1. **项目持久化 + 文件浏览器** - Agent 能写文件，用户能查看
2. **Agent 生成系统** - 手动配置 + AI 自动生成 Agent
3. **可视化预览** - 静态 HTML/CSS/JS 文件预览

## 技术方案：最小改动原则

利用 Claude Agent SDK 已有能力，只做必要扩展：
- 文件操作依赖 SDK 原生 Read/Write/Edit 工具
- Skill 使用 Claude Code 原生 `.claude` 目录机制
- Agent 配置用文件系统存储（YAML），不加数据库表

---

## 功能 1：项目持久化 + 文件浏览器（P0）

### 1.1 让 Agent 能写文件

**问题根因**：当前 `HelloAgent` 的 Claude Agent SDK 配置没有启用文件操作工具。

**解决方案**：修改 `HelloAgent.init_claude_option()` 配置：

```python
# apps/api/app/services/cli/adapters/hello_agent.py
def init_claude_option(self) -> ClaudeAgentOptions:
    return ClaudeAgentOptions(
        # 关键：设置工作目录为项目目录
        cwd=self.project_path,  # 新增参数，从 execute 时传入

        # 启用文件操作权限
        allowed_tools=["Read", "Write", "Edit", "Bash", "Glob", "Grep"],

        # 其他配置...
    )
```

**改动点**：
- `BaseCLI.execute_with_streaming()` 接收 `project_path` 参数
- 传递给 Claude Agent SDK 作为 `cwd`
- Agent 生成的所有文件写入 `data/projects/{project_id}/`

### 1.2 文件浏览器后端 API

新增文件系统 API（`apps/api/app/api/files.py`）：

```python
# 获取项目目录树
GET /api/projects/{project_id}/files
→ 返回: { "tree": [{ "name": "index.html", "type": "file", "path": "index.html" }, ...] }

# 读取文件内容
GET /api/projects/{project_id}/files/{file_path:path}
→ 返回: { "content": "...", "encoding": "utf-8" }

# （可选）保存文件
PUT /api/projects/{project_id}/files/{file_path:path}
→ 请求: { "content": "..." }
```

**安全考虑**：
- 路径校验防止目录遍历攻击（`..` 检测）
- 限制在 `data/projects/{project_id}/` 范围内

### 1.3 文件浏览器前端组件

在聊天页面左侧或右侧新增文件面板：

```
┌─────────────────────────────────────────────┐
│  项目名称                                    │
├──────────┬──────────────────────────────────┤
│ 📁 文件   │  聊天区域                         │
│ ├─ src/  │  ┌────────────────────────────┐  │
│ │  └─app │  │ [消息列表...]              │  │
│ ├─ index │  │                            │  │
│ └─ style │  └────────────────────────────┘  │
│          │  [输入框]                        │
└──────────┴──────────────────────────────────┘
```

**交互**：
- 点击文件 → 右侧显示文件内容（只读预览）
- 文件树实时刷新（Agent 写入新文件后通过 WebSocket 通知）
- 使用 `lucide-react` 图标区分文件类型

---

## 功能 2：Agent 生成系统（P1）

### 2.1 Agent 配置文件结构

**全局模板**（`extensions/agents/`）：

```
extensions/agents/
├── code-assistant/
│   ├── agent.yaml          # Agent 配置
│   └── .claude/
│       └── skills/
│           └── coding.md   # 关联的 skills
├── data-analyst/
│   ├── agent.yaml
│   └── .claude/
│       └── skills/
└── system-agent/           # 预制的"Agent 生成器"
    ├── agent.yaml
    └── .claude/
        └── skills/
            └── agent-creator.md
```

**agent.yaml 格式**：

```yaml
name: "Code Assistant"
description: "帮助编写和调试代码"
system_prompt: |
  你是一个专业的代码助手...

skills:
  - coding          # 引用 .claude/skills/ 下的 skill
  - debugging

model: "claude-sonnet-4-5-20250929"  # 可选，默认值
```

**项目级配置**（`data/projects/{id}/.claude/`）：

```
data/projects/abc123/
├── .claude/
│   ├── agent.yaml          # 项目专属 Agent 配置（覆盖全局）
│   └── skills/
│       └── custom-skill.md # 项目专属 skill
├── src/
└── index.html
```

### 2.2 配置加载逻辑

**优先级**（从高到低）：
1. 项目级 `{project}/.claude/agent.yaml`
2. 全局模板 `extensions/agents/{agent_type}/agent.yaml`
3. 代码中的默认配置

```python
# apps/api/app/services/cli/config_loader.py

def load_agent_config(project_path: str, agent_type: str) -> AgentConfig:
    # 1. 检查项目级配置
    project_config = f"{project_path}/.claude/agent.yaml"
    if exists(project_config):
        return parse_yaml(project_config)

    # 2. 检查全局模板
    global_config = f"extensions/agents/{agent_type}/agent.yaml"
    if exists(global_config):
        return parse_yaml(global_config)

    # 3. 返回默认
    return default_config(agent_type)
```

### 2.3 前端 Agent 配置 UI

在项目设置中新增 Agent 配置面板：

```
┌─────────────────────────────────────┐
│ Agent 配置                    [保存] │
├─────────────────────────────────────┤
│ 模板: [Code Assistant ▼]            │
│       ☐ 使用项目自定义配置           │
├─────────────────────────────────────┤
│ System Prompt:                      │
│ ┌─────────────────────────────────┐ │
│ │ 你是一个专业的代码助手...        │ │
│ └─────────────────────────────────┘ │
├─────────────────────────────────────┤
│ Skills:                             │
│ ☑ coding    ☑ debugging            │
│ ☐ testing   [+ 添加自定义]          │
└─────────────────────────────────────┘
```

**交互**：
- 选择全局模板 → 自动填充配置
- 勾选"自定义" → 保存到项目 `.claude/agent.yaml`
- Skills 可多选，也可添加项目专属 skill

### 2.4 System Agent 自动生成

**预制 System Agent 模板**：

```yaml
# extensions/agents/system-agent/agent.yaml
name: "System Agent"
description: "根据用户需求生成 Agent 配置"
system_prompt: |
  你是一个 Agent 配置专家。用户会描述他们需要什么样的 AI 助手，
  你需要：
  1. 理解用户需求
  2. 生成合适的 system prompt
  3. 推荐需要的 skills
  4. 将配置写入 .claude/agent.yaml

  配置格式示例：
  ```yaml
  name: "助手名称"
  description: "描述"
  system_prompt: |
    详细的 system prompt...
  skills:
    - skill1
    - skill2
  ```

skills:
  - agent-creator   # 包含 Agent 设计最佳实践
```

**使用流程**：

```
用户: "我需要一个能帮我写 React 组件的助手"

System Agent:
  1. 分析需求 → React 前端开发
  2. 生成配置:
     - system_prompt: React 专家角色设定
     - skills: [coding, react-patterns, testing]
  3. 写入 {project}/.claude/agent.yaml
  4. 回复用户配置已生成

用户: 切换到新生成的 Agent，开始使用
```

**前端入口**：

```
创建项目时:
○ 选择现有模板 [Code Assistant ▼]
● 让 AI 帮我创建 Agent → 进入 System Agent 对话
```

---

## 功能 3：可视化预览（P2）

### 3.1 后端静态文件服务

在 FastAPI 中添加静态文件服务：

```python
# apps/api/app/api/preview.py

from fastapi import APIRouter
from fastapi.responses import FileResponse

router = APIRouter()

# 为每个项目提供静态文件服务
# GET /api/preview/{project_id}/{file_path}
@router.get("/{project_id}/{file_path:path}")
async def serve_file(project_id: str, file_path: str):
    project_path = f"{PROJECTS_ROOT}/{project_id}"
    # 返回静态文件，自动处理 MIME 类型
    return FileResponse(f"{project_path}/{file_path}")
```

### 3.2 前端预览组件

在文件浏览器中，点击 HTML 文件时显示预览按钮：

```
┌─────────────────────────────────────────────┐
│ 📄 index.html                    [预览] [代码]│
├─────────────────────────────────────────────┤
│  ┌─────────────────────────────────────┐    │
│  │                                     │    │
│  │   <iframe 预览区域>                 │    │
│  │   src="/api/preview/{id}/index.html"│    │
│  │                                     │    │
│  └─────────────────────────────────────┘    │
└─────────────────────────────────────────────┘
```

**交互**：
- 点击「预览」→ iframe 加载 HTML 页面
- 点击「代码」→ 显示源代码
- 支持刷新按钮（Agent 更新文件后重新加载）
- 可全屏预览（新窗口打开）

### 3.3 自动检测可预览文件

```typescript
const PREVIEWABLE = ['.html', '.htm', '.svg'];

function isPreviewable(filename: string): boolean {
  return PREVIEWABLE.some(ext => filename.endsWith(ext));
}
```

---

## 实现计划

### 阶段 1：项目持久化 + 文件浏览器（P0）

| 任务 | 涉及文件 |
|------|---------|
| 修改 BaseCLI 接收 project_path | `apps/api/app/services/cli/base.py` |
| 修改 HelloAgent 启用文件工具 | `apps/api/app/services/cli/adapters/hello_agent.py` |
| 新增 files API | `apps/api/app/api/files.py` |
| 注册 files 路由 | `apps/api/app/main.py` |
| 新增文件树组件 | `apps/web/components/FileTree.tsx` |
| 修改聊天页面集成文件面板 | `apps/web/app/chat/[projectId]/page.tsx` |

### 阶段 2：Agent 生成系统（P1）

| 任务 | 涉及文件 |
|------|---------|
| 创建 config_loader | `apps/api/app/services/cli/config_loader.py` |
| 创建 agents API | `apps/api/app/api/agents.py`（扩展） |
| 创建全局模板目录结构 | `extensions/agents/` |
| 创建 System Agent 模板 | `extensions/agents/system-agent/` |
| 新增 Agent 配置 UI 组件 | `apps/web/components/AgentConfig.tsx` |
| 修改项目创建流程 | `apps/web/app/page.tsx` |

### 阶段 3：可视化预览（P2）

| 任务 | 涉及文件 |
|------|---------|
| 新增 preview API | `apps/api/app/api/preview.py` |
| 注册 preview 路由 | `apps/api/app/main.py` |
| 新增预览组件 | `apps/web/components/FilePreview.tsx` |
| 集成到文件浏览器 | `apps/web/components/FileTree.tsx` |

---

## 设计决策记录

| 决策 | 选项 | 选择 | 原因 |
|------|------|------|------|
| 整体方案 | 最小改动 vs 完整建模 | 最小改动 | YAGNI 原则，利用 SDK 已有能力 |
| Skill 机制 | 自定义 vs Claude Code 原生 | 原生 | 不重新发明轮子 |
| Agent 配置存储 | 数据库 vs 文件系统 | 文件系统 | 符合 Claude Code 模式 |
| 配置层级 | 单层 vs 双层 | 双层 | 全局模板 + 项目自定义 |
| 预览方案 | 静态服务 vs 开发服务器 | 静态服务 | 当前只需支持静态文件 |
